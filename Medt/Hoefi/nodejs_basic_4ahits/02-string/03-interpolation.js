const firstName = "Eduard";
const lastName = "Müller";
const street = "Anton Ehrenfried-Straße";
const housenr = 10;
const zipcode = "2020";
const city = "Hollabrunn";

// Template string mit interpolation
// template steht für schablone
const address = `${firstName} ${lastName}`
`${street} ${hoeseNr}`
`${zipCode} ${city}`;

console.log(address);